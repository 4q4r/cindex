import { Search, X } from 'lucide-react';

interface SearchBarProps {
  query: string;
  expression: string;
  onQueryChange: (q: string) => void;
  onExpressionChange: (v: string) => void;
  onSearch: () => void;
  isLoading: boolean;
}

const quickQueries = [
  'машинное обучение в диагностике',
  'CRISPR редактирование генов',
  'изменение климата биоразнообразие',
  'квантовая коррекция ошибок',
  'антибиотикорезистентность',
  'поиск темной материи',
];

const expressionOps = ['AND', 'OR', '-'];

export function SearchBar({
  query,
  expression,
  onQueryChange,
  onExpressionChange,
  onSearch,
  isLoading,
}: SearchBarProps) {
  const handleKeyDown = (e: React.KeyboardEvent) => {
    if (e.key === 'Enter' && !isLoading) {
      onSearch();
    }
  };

  const appendExpressionToken = (token: string) => {
    const trimmed = expression.trim();
    if (!trimmed) {
      onExpressionChange(token === '-' ? '-термин' : token);
      return;
    }

    if (token === '-') {
      onExpressionChange(`${trimmed} -термин`);
      return;
    }

    onExpressionChange(`${trimmed} ${token} `);
  };

  const wrapWithQuotes = () => {
    const value = expression.trim() || query.trim();
    if (!value) return;
    onExpressionChange(`"${value}"`);
  };

  return (
    <div className="sticky top-0 z-40 bg-bg-primary/90 backdrop-blur-sm border-b border-border-subtle">
      <div className="px-6 py-5">
        <div className="max-w-3xl">
          <div className="relative flex items-center gap-2">
            <div className="relative flex-1">
              <Search className="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-text-tertiary" />
              <input
                type="text"
                value={query}
                onChange={(e) => onQueryChange(e.target.value)}
                onKeyDown={handleKeyDown}
                placeholder="Тема, ключевики, DOI, автор..."
                className="w-full bg-bg-input border border-border-default rounded-lg pl-12 pr-10 py-3.5 text-text-primary placeholder:text-text-tertiary focus:outline-none focus:border-accent/50 focus:ring-1 focus:ring-accent/20 transition-colors text-[15px]"
                disabled={isLoading}
              />
              {query && (
                <button
                  onClick={() => onQueryChange('')}
                  className="absolute right-3 top-1/2 -translate-y-1/2 p-1 text-text-tertiary hover:text-text-secondary transition-colors"
                  aria-label="Очистить запрос"
                >
                  <X className="w-4 h-4" />
                </button>
              )}
            </div>
            <button
              onClick={onSearch}
              disabled={isLoading || !query.trim()}
              className="px-6 py-3.5 bg-accent hover:bg-accent-muted disabled:opacity-40 disabled:cursor-not-allowed text-white font-medium rounded-lg transition-colors text-sm shrink-0"
            >
              {isLoading ? 'Поиск...' : 'Искать'}
            </button>
          </div>

          <div className="mt-3">
            <label className="block text-[11px] uppercase tracking-wider text-text-tertiary mb-1.5">
              Выражение поиска (опционально)
            </label>
            <input
              type="text"
              value={expression}
              onChange={(e) => onExpressionChange(e.target.value)}
              onKeyDown={handleKeyDown}
              placeholder="Например: \"machine learning\" AND diagnosis -survey"
              className="w-full bg-bg-input border border-border-default rounded-lg px-3 py-2.5 text-sm text-text-primary placeholder:text-text-tertiary focus:outline-none focus:border-accent/50 focus:ring-1 focus:ring-accent/20 transition-colors"
              disabled={isLoading}
            />
            <div className="flex flex-wrap items-center gap-2 mt-2">
              <span className="text-[11px] text-text-tertiary uppercase tracking-wider mr-1">Билдер:</span>
              {expressionOps.map((op) => (
                <button
                  key={op}
                  type="button"
                  onClick={() => appendExpressionToken(op)}
                  className="px-2.5 py-1 text-[11px] text-text-secondary bg-bg-elevated border border-border-default rounded-md hover:bg-bg-hover hover:text-text-primary"
                >
                  {op === '-' ? 'NOT (-)' : op}
                </button>
              ))}
              <button
                type="button"
                onClick={wrapWithQuotes}
                className="px-2.5 py-1 text-[11px] text-text-secondary bg-bg-elevated border border-border-default rounded-md hover:bg-bg-hover hover:text-text-primary"
              >
                "точная фраза"
              </button>
            </div>
          </div>

          <div className="flex items-center gap-4 mt-2.5 text-[11px] text-text-tertiary">
            <span>"точная фраза" для точного совпадения</span>
            <span className="text-border-default">|</span>
            <span>AND / OR для булевой логики</span>
            <span className="text-border-default">|</span>
            <span>-термин для исключения</span>
          </div>

          <div className="flex flex-wrap items-center gap-2 mt-3.5">
            <span className="text-[11px] text-text-tertiary uppercase tracking-wider mr-1">Примеры:</span>
            {quickQueries.map((q) => (
              <button
                key={q}
                onClick={() => {
                  onQueryChange(q);
                  onExpressionChange('');
                }}
                className="px-3 py-1 text-xs text-text-secondary bg-bg-elevated border border-border-default rounded-full hover:bg-bg-hover hover:text-text-primary hover:border-accent/30 transition-all"
              >
                {q}
              </button>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
